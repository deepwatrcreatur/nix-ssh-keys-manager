Status: `ready`
Suggested branch: `test/ssh-keys-manager-nixos-vm`
Priority: `high`

# NixOS VM Integration Test

## Goal

Add a proper NixOS VM test that validates real file placement, ownership, and
`sshd` integration for `services.ssh-keys-manager`.
